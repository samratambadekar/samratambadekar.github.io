Works on Chrome and Firefox. Might not work on IE/Safari/Opera.
The animations tend to lag on Firefox for Mobile.

The list gets populated with limited entries due to api limit.
Users location is approximate (based on browser geolocation). GPS tracking is avoided to have consistency over phone and PC (also privacy). Some ad blockers / script blockers might block browser geolocation.